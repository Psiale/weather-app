function Weather(mainWeather, weatherDescription, temp, tempMin, tempMax, icon) {
  return {
    mainWeather,
    weatherDescription,
    temp,
    tempMin,
    tempMax,
    icon,
  };
}
export default Weather;